import Button from "react-bootstrap/Button"

export default function(){
    return <div>
        <Button variant = "primary">create list</Button>
        <Button variant = "secondary">toggle archive</Button>
    </div>
}